/** let num = prompt("Enter a number:");
      switch (true) {
        case num > 0:
          console.log("number is positive");
          break;
        case num == 0:
          document.write("number is zero");
          break;
        case num < 0:
          document.write("number is negative");
          break;
        default:
          document.write("invalid input");
      }**/
      /**let day = prompt("Enter a the day");
      switch (day) {
        case "monday":
        case "tuesday":
        case "wednesday":
        case "thursday":
          console.log("its a week day");
          break;
        case "friday":
        case "sunday":
        case "saturday":
          console.log("its a weekened");
          break;
        default:
          console.log("its an invalid day");
      }**/
